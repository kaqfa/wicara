# WICARA CMS Configuration
# Copy this file to .env and modify values as needed

# Flask Environment
# Options: development, production, testing
FLASK_ENV=development

# Secret Key for Session Management
# CRITICAL: Change this in production!
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-in-production

# Server Configuration
HOST=0.0.0.0
PORT=5555

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
LOG_FILE=logs/wicara.log

# Database Configuration (if needed in future)
# DATABASE_URL=sqlite:///wicara.db

# Email Configuration (if needed in future)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# Engine-Content Separation Configuration
# Controls how Wicara separates engine code from site content

# SITES_DIR: Base directory for all sites (default: sites)
# This directory will contain subdirectories for each site
SITES_DIR=sites

# DEFAULT_SITE: Default site identifier (default: default)
# Used when no specific site is requested or in single-site mode
DEFAULT_SITE=default

# LEGACY_MODE: Use legacy root paths (default: false for new installations)
# - true: Uses root paths (config.json, templates/, static/)
#   Compatible with pre-ECS installations
# - false: Uses sites/ structure (sites/default/config.json, etc.)
#   Recommended for new installations, enables multi-site support
# Set to 'false' for new installations, 'true' for existing installations
LEGACY_MODE=false
